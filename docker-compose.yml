services:
  at-fan:
    build:
      context: .
    container_name: at-fan-web
    restart: unless-stopped
    ports:
      - "${AT_FAN_PORT:-18080}:80"
    networks:
      - proxy

networks:
  proxy:
    external: true
